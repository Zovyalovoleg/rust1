#[test]
fn main() {
    let x: i32 = 5; // Инициализация переменной x значением 5
    let y: i32; // Переменная y не используется, предупреждение остаётся

    assert_eq!(x, 5);
    println!("Success!");
}
